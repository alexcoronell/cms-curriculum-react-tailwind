/* Components */
import BasicPage from "../../Components/BasicPage";

const BasicData = () => {
  const titlePage = "BasicData";

  return (
    <BasicPage titlePage={titlePage}>
      <div className="BasicData content">BasicData</div>
    </BasicPage>
  );
};

export default BasicData;
